[package]
name = "dungeon-core"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy_ecs = "0.16"
rand = "0.10.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
symmetric-shadowcasting = "0.2"
